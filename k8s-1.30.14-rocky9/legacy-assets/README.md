# Legacy Assets

This directory preserves historical installation assets for comparison only.

These manifests have not passed the current security baseline and final compatibility validation. Do not install them. Use assets collected by `scripts/download_assets_offline.sh` and install with `scripts/install.sh` from the package root.
